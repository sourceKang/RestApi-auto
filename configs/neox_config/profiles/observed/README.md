# NeoX Observed Profile Behavior

Reserved for per-profile live-device observations and known endpoint limits.

Use this for exploratory findings, rejected payloads, device-specific behavior, and delete restrictions. Do not load these records into official success tests unless they are promoted to `minmax/` or `invalid/`.
